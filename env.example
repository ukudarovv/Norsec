# Stage 9 — API / PostgreSQL
DATABASE_URL=postgresql+psycopg2://bullying_ai:bullying_ai@127.0.0.1:5432/bullying_ai
JWT_SECRET=replace-with-long-random-secret-min-32-chars
ACCESS_TOKEN_EXPIRE_MINUTES=1440
CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# Optional: first user already exists — open self-signup (dev only)
# ALLOW_OPEN_REGISTRATION=1
# Optional: create tables without Alembic (dev only)
# AUTO_CREATE_TABLES=1
