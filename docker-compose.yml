services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: bullying_ai
      POSTGRES_USER: bullying_ai
      POSTGRES_PASSWORD: bullying_ai
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
